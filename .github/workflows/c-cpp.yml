name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Install Clang 18 and Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-18 g++-14

      - name: Configure CMake
        env:
          CC: clang-18
          CXX: clang++-18
        run: cmake -B build -S . -DCMAKE_BUILD_TYPE=Release

      - name: Compiling
        run: cmake --build build --config Release

      - name: Run
        run: ./build/mygit